services:
  bands-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bands-api
    ports:
      - "8080:8080"
    environment:
      JAVA_OPTS: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"
    restart: unless-stopped

